self.esm_encoder = nn.TransformerEncoder(
    encoder_layer,
    num_layers=2,
    enable_nested_tensor=False
)
self.esm_encoder = nn.TransformerEncoder(
    encoder_layer,
    num_layers=2,
    enable_nested_tensor=False
)
